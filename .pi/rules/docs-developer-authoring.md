---
paths: "docs/src/content/docs/developer/**/*.{md,mdx}"
skills: astro-docs-authoring
---
Write system documentation: architecture, protocol contracts, internal state, design decisions, contributor knowledge, and lesson-learnt reports.
Place each page under `developer/<domain>/`; add `<subdomain>/` only when the domain needs another stable grouping level.
Keep operator procedures and service usage in the `user/` namespace.

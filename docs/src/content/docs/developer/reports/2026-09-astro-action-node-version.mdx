---
title: Astro action defaults to unsupported Node 20
description: GitHub Pages build fails “Node.js v20.20.2 is not supported by Astro” because withastro/action v3 defaults to Node 20 while Astro 7 requires Node 22.12 or newer.
date: 2026-09-09
severity: medium
tags: [tech/astro, tech/github-actions, kind/vendor-quirk, area/ci]
sidebar:
  label: 2026-09-09 Astro action Node version
  order: -1788921750824
---

## Symptom

```txt
Node.js v20.20.2 is not supported by Astro!
Please upgrade Node.js to a supported version: ">=22.12.0"
```

The GitHub Pages build job failed inside `withastro/action@v3`; deployment was skipped.

## Context

The documentation site uses Astro 7. Local builds ran on Node 24 and passed. GitHub Actions used the composite action's internal default Node version instead of the local runtime.

Failed run: [34207278596](https://github.com/tigorlazuardi/pi-messaging-relay/actions/runs/34207278596).

## Offending code

```yaml title=".github/workflows/docs.yml" showLineNumbers startLineNumber=18 {1-3}
      - uses: withastro/action@v3
        with:
          path: ./docs # action silently defaults node-version to 20
```

## Root cause

`withastro/action@v3` declares its `node-version` input with default value `20`. Astro 7 requires Node `>=22.12.0`, so dependency installation completed with engine warnings and `astro build` then exited immediately.

A separate `setup-node` step would not be authoritative because the composite action runs its own `actions/setup-node` using its input.

## Fix

Pass the supported runtime directly to the Astro action:

```diff
       - uses: withastro/action@v3
         with:
           path: ./docs
+          node-version: 24
```

## Lesson

Pin `node-version` on `withastro/action`, matching the documentation site's supported runtime. Do not rely on the action's default or the runner's preinstalled Node version.

## Recurrence log

| Date | Where | Trace ID | Notes |
|---|---|---|---|
| 2026-09-09 | GitHub Pages build run 34207278596 | — | First occurrence; action defaulted to Node 20. |
